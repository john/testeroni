require 'rails_helper'

RSpec.describe Tst, type: :model do
  # describe 'public lists' do
  #   it 'should return public lists' do
  #     create(:public_list)
  #     public_lists = List.is_public
  #     expect(public_lists.length).to eq(1)
  #   end
  #
  #   it 'should have correct status' do
  #     l = create(:public_list)
  #     expect(l.is_public?).to eq( true )
  #     expect(l.is_private?).to eq( false )
  #   end
  # end
  #
  # describe 'private lists' do
  #   it 'should have correct status' do
  #     l = create(:private_list)
  #     expect(l.is_public?).to eq( false )
  #     expect(l.is_private?).to eq( true )
  #   end
  # end
end
